+ Application 

1- in the application create template 

>> templates -> index.html 

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
    <h1>Hello Vibol</h1>
</body>
</html>

2- in the views.py : where we get the template then render it 

from django.shortcuts import render

def index(request):
    return render(request, "index.html")

3- Create urls.py in the application : it like the route where it get the path 

from django.urls import path
from . import views

urlpatterns = [
    path('', views.index, name = 'index')
]


** in the project urls.py (route ) **

- add the application route 

from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('job_application.urls'))
]
























